import { SemaphoreGroupPCD, SemaphoreGroupPCDClaim } from '@pcd/semaphore-group-pcd'

  /*
    merkleRoot: string;
    depth: number;
    signal: string;
    externalNullifier: string;
    nullifierHash: string;
   */

  ; (async () => {
    const id = ''
    const claim = ''
    const pcd = new SemaphoreGroupPCD()
  })()
